<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- Tree view -->
    <record id="view_task_tree" model="ir.ui.view">
        <field name="name">olearn2.task.tree</field>
        <field name="model">olearn2.task</field>
        <field name="arch" type="xml">
            <tree decoration-muted="hidden==True">
                <field name="course_id"/>
                <field name="lesson_id"/>
                <field name="name"/>
                <field name="type"/>
                <field name="due_date"/>
                <field name="max_score"/>
                <field name="hidden" widget="boolean_toggle"/>
                <field name="assigned_count" string="Assigned To"/>
            </tree>
        </field>
    </record>

    <!-- Kanban view -->
    <record id="view_task_kanban" model="ir.ui.view">
        <field name="name">olearn2.task.kanban</field>
        <field name="model">olearn2.task</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_small_column" default_group_by="course_id" group_create="false">
                <field name="name"/>
                <field name="course_id"/>
                <field name="type"/>
                <field name="due_date"/>
                <field name="max_score"/>
                <field name="hidden"/>
                <field name="assigned_count"/>

                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_card oe_kanban_global_click">
                            <div class="o_kanban_card_header">
                                <div class="o_kanban_card_header_title">
                                    <div class="o_primary">
                                        <strong>
                                            <field name="name"/>
                                        </strong>
                                    </div>
                                    <div class="o_secondary">
                                        <span class="badge badge-primary">
                                            <field name="type"/>
                                        </span>
                                        <span t-if="record.hidden.raw_value" class="badge badge-warning ml-1">
                                            <i class="fa fa-eye-slash"/>
                                            Hidden
                                        </span>
                                    </div>
                                </div>
                            </div>

                            <div class="o_kanban_card_body">
                                <div>
                                    <i class="fa fa-book"/>
                                    Course:
                                    <field name="course_id"/>
                                </div>
                                <div class="mt-1">
                                    <i class="fa fa-calendar"/>
                                    Due:
                                    <field name="due_date"/>
                                </div>
                                <div class="mt-1">
                                    <i class="fa fa-trophy"/>
                                    Max Score:
                                    <field name="max_score"/>
                                </div>
                                <div class="mt-1">
                                    <i class="fa fa-users"/>
                                    Assigned to
                                    <field name="assigned_count"/>
                                    students
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Form view -->
    <record id="view_task_form" model="ir.ui.view">
        <field name="name">olearn2.task.form</field>
        <field name="model">olearn2.task</field>
        <field name="arch" type="xml">
            <form string="Task">
                <header>
                    <button string="Publish to Students"
                            name="action_publish_to_students"
                            type="object"
                            class="oe_highlight"
                            attrs="{'invisible': [('hidden', '=', False)]}"/>
                    <button string="Hide from Students"
                            name="action_hide_from_students"
                            type="object"
                            attrs="{'invisible': [('hidden', '=', True)]}"/>
                    <field name="active" widget="boolean_button" options="{'terminology': 'archive'}"/>
                </header>

                <sheet>

                    <widget name="web_ribbon" title="Hidden" bg_color="bg-warning"
                            attrs="{'invisible': [('hidden', '=', False)]}"/>

                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="e.g., Python Exercise 1"/>
                        </h1>
                    </div>

                    <group>
                        <group>
                            <field name="course_id" options="{'no_create': True}"/>
                            <field name="lesson_id" domain="[('course_id', '=', course_id)]"/>
                            <field name="type"/>
                        </group>
                        <group>
                            <field name="due_date"/>
                            <field name="max_score"/>
                            <field name="hidden" widget="boolean_toggle"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Instructions" name="content">
                            <field name="content" widget="html" placeholder="Task instructions and requirements..."/>
                        </page>
                        <page string="Student Tasks" name="student_tasks">
                            <field name="task_record_ids">
                                <tree decoration-danger="status=='assigned' and overdue==True"
                                      decoration-success="status=='graded'">
                                    <field name="student_id"/>
                                    <field name="status"/>
                                    <field name="score"/>
                                    <field name="submission_date"/>
                                    <field name="overdue" invisible="1"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search view -->
    <record id="view_task_search" model="ir.ui.view">
        <field name="name">olearn2.task.search</field>
        <field name="model">olearn2.task</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="course_id"/>

                <filter string="Visible to Students" name="visible" domain="[('hidden', '=', False)]"/>
                <filter string="Hidden from Students" name="hidden" domain="[('hidden', '=', True)]"/>

                <separator/>
                <filter string="Problems" name="problems" domain="[('type', '=', 'problem')]"/>
                <filter string="Writing" name="writing" domain="[('type', '=', 'writing')]"/>
                <filter string="Quizzes" name="quizzes" domain="[('type', '=', 'quiz')]"/>

                <group expand="0" string="Group By">
                    <filter string="Course" name="group_course" context="{'group_by': 'course_id'}"/>
                    <filter string="Type" name="group_type" context="{'group_by': 'type'}"/>
                    <filter string="Due Date" name="group_due_date" context="{'group_by': 'due_date'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Actions -->
    <record id="action_task_manager" model="ir.actions.act_window">
        <field name="name">All Tasks</field>
        <field name="res_model">olearn2.task</field>
        <field name="view_mode">kanban,tree,form</field>
        <field name="search_view_id" ref="view_task_search"/>
        <field name="context">{'search_default_group_course': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No tasks found.
            </p>
            <p>
                Click <b>Create</b> to add your first task.
            </p>
        </field>
    </record>

    <!--<field name="domain">[('teacher_id', 'in', [uid])]</field>-->
    <record id="action_task_teacher" model="ir.actions.act_window">
        <field name="name">My Tasks</field>
        <field name="res_model">olearn2.task</field>
        <field name="view_mode">kanban,tree,form</field>
        <field name="search_view_id" ref="view_task_search"/>
        <field name="context">{'search_default_group_course': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                You haven't created any tasks yet.
            </p>
            <p>
                Create tasks from your programs to get started!
            </p>
        </field>
    </record>

</odoo>