<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- Tree view -->
    <record id="view_lesson_tree" model="ir.ui.view">
        <field name="name">olearn2.lesson.tree</field>
        <field name="model">olearn2.lesson</field>
        <field name="arch" type="xml">
            <tree decoration-muted="hidden==True">
                <field name="sequence" widget="handle"/>
                <field name="course_id"/>
                <field name="name"/>
                <field name="type"/>
                <field name="hidden" widget="boolean_toggle"/>
            </tree>
        </field>
    </record>

    <!-- Kanban view -->
    <record id="view_lesson_kanban" model="ir.ui.view">
        <field name="name">olearn2.lesson.kanban</field>
        <field name="model">olearn2.lesson</field>
        <field name="arch" type="xml">
            <kanban default_group_by="course_id" group_create="false" class="o_kanban_small_column">
                <field name="name"/>
                <field name="course_id"/>
                <field name="type"/>
                <field name="hidden"/>

                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_card oe_kanban_global_click"
                             t-attf-class="#{record.hidden.raw_value ? 'o_kanban_card_fancy' : ''}">
                            <div class="o_kanban_card_header">
                                <div class="o_kanban_card_header_title">
                                    <div class="o_primary">
                                        <strong>
                                            <field name="name"/>
                                        </strong>
                                    </div>
                                    <div class="o_secondary">
                                        <span class="badge"
                                              t-att-class="record.type.raw_value == 'video' ? 'badge-primary' : record.type.raw_value == 'material' ? 'badge-info' : 'badge-secondary'">
                                            <field name="type"/>
                                        </span>
                                        <span t-if="record.hidden.raw_value" class="badge badge-warning ml-1">
                                            <i class="fa fa-eye-slash"/>
                                            Hidden
                                        </span>
                                    </div>
                                </div>
                            </div>

                            <div class="o_kanban_card_body">
                                <div>
                                    <i class="fa fa-book"/>
                                    Course:
                                    <field name="course_id"/>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Form view -->
    <record id="view_lesson_form" model="ir.ui.view">
        <field name="name">olearn2.lesson.form</field>
        <field name="model">olearn2.lesson</field>
        <field name="arch" type="xml">
            <form string="Lesson">
                <header>
                    <button string="Publish to Students"
                            name="action_publish_to_students"
                            type="object"
                            class="oe_highlight"
                            attrs="{'invisible': [('hidden', '=', False)]}"/>
                    <button string="Hide from Students"
                            name="action_hide_from_students"
                            type="object"
                            attrs="{'invisible': [('hidden', '=', True)]}"/>
                </header>

                <sheet>

                    <widget name="web_ribbon" title="Hidden" bg_color="bg-warning"
                            attrs="{'invisible': [('hidden', '=', False)]}"/>

                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="e.g., Introduction to Python"/>
                        </h1>
                    </div>

                    <group>
                        <group>
                            <field name="course_id" options="{'no_create': True}"/>
                            <field name="type"/>
                            <field name="sequence"/>
                        </group>
                        <group>
                            <field name="hidden" widget="boolean_toggle"/>
                        </group>
                    </group>

                    <notebook>
                        <page string="Content" name="content">
                            <group>
                                <field name="content" widget="html"
                                       placeholder="Lesson description and instructions..."/>
                                <field name="link" placeholder="https://... or file path"/>
                            </group>
                        </page>
                        <page string="Student Lessons" name="student_lessons">
                            <field name="lesson_record_ids">
                                <tree>
                                    <field name="student_id"/>
                                    <field name="viewed" widget="boolean_toggle"/>
                                    <field name="viewed_date"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search view -->
    <record id="view_lesson_search" model="ir.ui.view">
        <field name="name">olearn2.lesson.search</field>
        <field name="model">olearn2.lesson</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="course_id"/>

                <filter string="Visible to Students" name="visible" domain="[('hidden', '=', False)]"/>
                <filter string="Hidden from Students" name="hidden" domain="[('hidden', '=', True)]"/>

                <separator/>
                <filter string="Videos" name="videos" domain="[('type', '=', 'video')]"/>
                <filter string="Materials" name="materials" domain="[('type', '=', 'material')]"/>
                <filter string="Examples" name="examples" domain="[('type', '=', 'example')]"/>

                <group expand="0" string="Group By">
                    <filter string="Course" name="group_course" context="{'group_by': 'course_id'}"/>
                    <filter string="Type" name="group_type" context="{'group_by': 'type'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- Actions -->
    <record id="action_lesson_manager" model="ir.actions.act_window">
        <field name="name">All Lessons</field>
        <field name="res_model">olearn2.lesson</field>
        <field name="view_mode">tree,kanban,form</field>
        <field name="search_view_id" ref="view_lesson_search"/>
        <field name="context">{'search_default_group_course': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No lessons found.
            </p>
            <p>
                Click <b>Create</b> to add your first lesson.
            </p>
        </field>
    </record>

    <record id="action_lesson_teacher" model="ir.actions.act_window">
        <field name="name">My Lessons</field>
        <field name="res_model">olearn2.lesson</field>
        <field name="view_mode">kanban,tree,form</field>
        <field name="search_view_id" ref="view_lesson_search"/>
        <field name="domain">[('course_id.teacher_id', '=', uid)]</field>
        <field name="context">{'search_default_group_course': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                You haven't created any lesson yet.
            </p>
            <p>
                Create lessons from your course to get started!
            </p>
        </field>
    </record>

</odoo>